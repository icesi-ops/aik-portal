const { assert } = require('chai');

describe('Testeo del test', () => {
  describe('Assert', () => {
    it('Este assert tiene como proposito verificar que se puede testear el portal', function() {
      assert.equal(-1, -1);
    });
  });
});